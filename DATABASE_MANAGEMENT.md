# 📊 مدیریت دیتابیس

## وضعیت فعلی

- **تعداد کارمندان**: 136
- **دیتابیس**: SQLite (`employees.db`)
- **ستون‌های جدول**:
  - `id`: شناسه منحصر
  - `name_en`: نام انگلیسی
  - `name_fa`: نام فارسی
  - `title_en`: عنوان انگلیسی
  - `title_fa`: عنوان فارسی
  - `dept_en`: بخش انگلیسی
  - `dept_fa`: بخش فارسی
  - `extension`: داخلی
  - `mobile`: موبایل
  - `email`: ایمیل
  - `photo`: عکس (URL)
  - `icon`: جنسیت (male/female/unknown)
  - `visible`: نمایش (1=نمایش / 0=عدم نمایش)

---

## اضافه کردن/بروزرسانی اطلاعات

### روش 1: استفاده از DB Browser for SQLite (ساده‌ترین)

1. دانلود: https://sqlitebrowser.org/
2. باز کنید: `backend/employees.db`
3. بر روی جدول `employees` کلیک کنید
4. داده‌ها را ویرایش یا اضافه کنید
5. Save کنید

### روش 2: استفاده از Command Line

```bash
cd backend
sqlite3 employees.db
```

سپس SQL commands:

```sql
-- نمایش تمام کارمندان
SELECT id, name_en, extension, visible FROM employees;

-- مخفی کردن یک کارمند
UPDATE employees SET visible = 0 WHERE id = 5;

-- نمایش مجدد یک کارمند
UPDATE employees SET visible = 1 WHERE id = 5;

-- اضافه کردن کارمند جدید
INSERT INTO employees (name_en, name_fa, title_en, title_fa, dept_en, dept_fa, extension, mobile, email, icon, visible)
VALUES ('Ali', 'علی', 'Manager', 'مدیر', 'IT', 'فناوری اطلاعات', '2000', '09123456789', 'ali@company.com', 'male', 1);

-- حذف کارمند
DELETE FROM employees WHERE id = 145;
```

---

## ستون `visible` (وضعیت نمایش)

### مقادیر:
- `1` = کارمند **نمایش داده می‌شود**
- `0` = کارمند **مخفی می‌شود**

### مثال:

```sql
-- مخفی کردن تمام کارمندان بخش IT
UPDATE employees SET visible = 0 WHERE dept_en = 'IT';

-- نمایش فقط کارمندان با عنوان Manager
UPDATE employees SET visible = 0 WHERE title_en != 'Manager';
UPDATE employees SET visible = 1 WHERE title_en = 'Manager';

-- نمایش کارمندان که جنسیت آن‌ها معلوم است
UPDATE employees SET visible = 1 WHERE icon IN ('male', 'female');
```

---

## بروزرسانی اطلاعات از JSON

اگر فایل `public/employees.json` را تغییر دهید و می‌خواهید دیتابیس را بروزرسانی کنید:

```bash
cd backend
npm run import
```

این کار:
- تمام داده‌های موجود را پاک می‌کند
- تمام داده‌های جدید را از JSON وارد می‌کند
- جنسیت را خودکار بر اساس عکس تشخیص می‌دهد
- تمام کارمندان را به صورت پیش‌فرض "نمایش" تنظیم می‌کند

---

## نکات مهم

⚠️ **هنگام ویرایش دیتابیس:**
- مطمئن شوید Backend **متوقف** است (Ctrl+C در terminal backend)
- فیلد `extension` باید **منحصر به فرد** باشد (تکراری نباید باشد)
- فیلد `visible` فقط `0` یا `1` قبول می‌کند

✅ **بهترین روش:**
1. تغییرات را در DB Browser انجام دهید
2. Backend را دوباره اجرا کنید
3. سایت را Refresh کنید

---

## مثال عملی: مخفی کردن مجموعه کارمندان

```sql
-- مخفی کردن کارمندانی که ایمیل ندارند
UPDATE employees SET visible = 0 WHERE email = '';

-- نمایش مجدد کارمندان خاص
UPDATE employees SET visible = 1 WHERE id IN (1, 5, 10);
```

---

## سوالات متکرر

### Q: چطور می‌توانم تمام کارمندان را مخفی کنم؟
```sql
UPDATE employees SET visible = 0;
```

### Q: چطور می‌توانم بدانم کدام کارمندان مخفی هستند؟
```sql
SELECT id, name_en, name_fa, extension FROM employees WHERE visible = 0;
```

### Q: آیا می‌توانم فقط بخش خاصی را مخفی کنم؟
```sql
UPDATE employees SET visible = 0 WHERE dept_en = 'HR';
```

---

**نیاز به کمک بیشتر؟** دستورات SQL را کسی آموزش بدهم یا فایل دیگری بسازم؟
